# 《剑来》MUD 开发手记 —— 问题与经验

> 记录开发过程中真实踩过的坑与对应的解法。项目形态：**单文件 HTML 文字 MUD**（`index.html`，约 1.7MB，零依赖零外链），数据驱动 + 运行时生成。

---

## 一、架构级教训

### 1. PRISTINE 快照陷阱（最隐蔽的一课）
`freshGame()` 用 `game = JSON.parse(JSON.stringify(PRISTINE_GAME))` 整体重置游戏状态，而 `PRISTINE_GAME` 是对初始 `game` 字面量的快照。
- **事故**：新手引导的 `tutFlags` 只在脚本加载时打补丁（`if(!game.tutFlags) game.tutFlags={}`），开新档重置后即丢失；`tutHint()` 读 `game.tutFlags.look` 抛错，又被 `look()` 的 try/catch 静默吞掉——引导条永远空白，且**无任何报错**。
- **规矩**：任何要跨开局存续的 `game.*` 新字段，**必须写进初始字面量**（快照之前）；要么就在使用处自愈兜底。给 game 加字段后自查两处。

### 2. 空绑定中断 init（黑屏事件）
删除 UI 元素（如战斗演出层）时，若 `init()` 里的 `addEventListener` 还绑着已删除的 id，会静默中断后续所有初始化——标题屏不显示、全按钮死、无可见报错。
- **规矩**：删 UI 必须同步删绑定；排查此类问题用 `<head>` 注入 `window.__bootErrs` 启动期错误探针（页面内的 error 监听装得太晚，抓不到启动期错误）。

### 3. 多批次合并的覆盖与 TDZ
- 多个 `Object.assign(NPC_DEEP, {...})` 后置批次会**整键覆盖**先前的合并结果——合并代码必须放在**所有批次之后**。
- 合并代码不得早于 `const QUESTS` 声明（TDZ 直接语法期炸）。

### 4. 重名冲突
新增 `TRADE_GOODS` 常量时与游戏既有的同名表撞车直接语法错。**加任何新系统前必须先全局搜索同名/同功能系统**。

---

## 二、Bug 排查方法论

### 5. 静默吞错是最大的敌人
`try/catch` 吞掉异常 + 渲染层静默失败 = 功能"消失"却无从查起。本轮两个典型：
- 引导条空白（tutFlags undefined 抛错被吞）；
- 交付文戏选项的 `effect` 被引擎映射函数整体替换丢弃（老任务的收尾台词从未生效过）。
- **解法**：浏览器里手动调用可疑函数、捕获异常消息（`try{ fn(); }catch(e){ out.err=e.message; }`），一步一步贴着数据走；不要靠读代码猜。

### 6. 测试要打在真实渲染层
- 交付文戏（playScene）的选项按钮渲染在 **#log** 里而非 #modal——测试点错容器，误判"流程没跑"。
- SVG 地图节点无 a11y 角色，模拟点击用 `t.closest('g').dispatchEvent(new MouseEvent('click',{bubbles:true}))`。
- `window.game` 取不到（`let` 声明不挂 window）——evaluate 里直接引用 `game`。

### 7. 语法校验三件套（每次批量改动后必跑）
`_tutorials/syntax_check.js`（vm.Script 定位行号）+ brace_scan + 浏览器 `__bootErrs`。生成器织入后必须跑——本轮生成器自身曾有 62 处 `。')` 笔误，靠 vm.Script 行号秒定位。

### 8. Windows 环境细节
- `findstr` 处理中文不可靠 → 全部改用 Node 脚本（`_tutorials/*.js`），输出走文件再 Read（cmd 管道吞中文输出）。
- `node -e "..."` 内嵌复杂正则/中文时 cmd 引号转义易翻车——写脚本文件跑，别赌内联。
- `cp`/`sleep`/`tail` 不存在——用 `copy`、`ping -n N 127.0.0.1 >nul`、Node 脚本替代。

---

## 三、设计与内容工程

### 9. 生成器模式 = 内容规模的杠杆
GONGFA_GEN（144 功法）、ITEM_GEN（120 装备）、QUESTS_HUB（267 任务）、QUESTS_CHAIN（140×5 节点）全部由**配置表 + 模板 + 确定性种子**生成，再以哨兵块（`//<<X-BEGIN>>…//<<X-END>>`）幂等织入 index.html。要点：
- 生成器解析真实游戏数据（ROOMS/NPCS/SECTS）再产出，保证引用的 NPC/房间/敌人都真实存在；
- 生成条目必须带全字段（本轮生成任务漏了 acceptOptions → playScene 空选项 = 软锁；漏 desc → 面板显示 undefined）；
- 幂等：重复运行走替换分支。

### 10. 任务设计的落地规范（源自《设计.txt》+ 放置江湖调研）
- 钩子前置：威胁型/欲望型写在对话前三句，先回答"玩家为什么要帮你"；
- 目标单一、链式推进（五节点：除狼→采药→探路→拜会→护镖），类型混搭防疲劳；
- 奖励必含叙事奖励（信息/解锁/侠名），物质奖励走银两体系；
- **跨章回执道具**是性价比最高的长线感设计：本章一枚"不值钱"的信物，N 章后找特定人物兑现大额潜能。

### 11. 原著核验优于记忆
境界名次序（练气登山五境、武夫炼气三境"英魂/雄魄/武胆"）全部以《剑来》全文检索核验后再定稿——记忆里"燕耳境""英魄境"等想当然的说法被原文逐条纠正。改世界观文本前先 grep 原著。

### 12. 参考代码库的编码坑
mhsj 等 LPC mudlib 文件是 GBK 编码，UTF-8 直读时中文全成乱码——用 latin1 读 + **ASCII 标识符**（potential/improve_skill/combat_exp）定位逻辑，中文关键词搜索会静默失败。

---

## 四、性能与体验

### 13. 移动端 flex 溢出
`#log{min-height:52%}` 在自动高度父容器里把整个圆框顶出屏幕，底部文字永远够不着。**百分比 min-height 在 auto-height flex 链上是毒药**——改 0 交给 flex 分配，父容器补 `overflow:hidden` 让"圆框即边界"。弹层/抽屉用 `dvh` 对抗移动端地址栏。

### 14. 交互边界要"框即世界"
舆图点击他城曾直接 `enterCheng` 免费瞬移——与逐格赶路的核心设计冲突。修复成三选一（徒步/驿路/传送阵），**免费方案保留但要求付出时间与风险**，世界感由此立住。

### 15. 统一入口优于散点补丁
引导置位收口为 `tutSet()`（置位+即时刷新）、任务解锁收口为 `discoverQuests()`（心跳扫描+questOpen 标记）——散在十处的 `game.tutFlags.x=1` 是 bug 温床。

---

## 五、流程经验

16. **先备份再动刀**：bak~bak5 按里程碑留档，出问题秒回滚；git 提交是第二保险。
17. **生成器幂等**：哨兵块 + 替换式写入，重跑不出重复数据。
18. **每次批量改动后固定流程**：syntax_check → brace_scan → 浏览器开新档冒烟 → 关键链路 evaluate 断言。
19. **文档即验收**：更新日志按版本写"根因+修法+回归验证"，倒逼把每个修复合上闸。
20. **网络抖动**：GitHub push 偶发 443 重置——本地 commit 先落袋，等 30-120 秒重试即可，切勿 force 操作。
